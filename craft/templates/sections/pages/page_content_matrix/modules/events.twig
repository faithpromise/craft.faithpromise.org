{% set entries = craft.entries({ section: 'events', with: 'eventImageTall' }) %}

{% if entries | length %}

    {% embed '_layout/section.twig' with params %}
        {% block body %}
            {% include '_layout/components/entry_cards.twig' with { 'entries': entries } %}
        {% endblock %}
    {% endembed %}

{% endif %}