{% set items = craft.entries({
section: 'events',
with: 'eventImage'
}) %}

{% if items | length %}

    {% embed '_layout/section.twig' with params %}
        {% block body %}
            {% include '_items/_cards.twig' with { 'items': items } %}
        {% endblock %}
    {% endembed %}

{% endif %}