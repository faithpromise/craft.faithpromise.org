{% set params = {
'image': item.eventImage[0],
'title': item.title,
'subtitle': item.dateRange,
'text': item.excerpt,
'action_url': item['theUrl'] is defined and item.theUrl | length ? item.theUrl : item.getUrl(),
'action_text': item.linkText | length ? item.linkText : 'More Details'
} %}

{% include '_items/_card.twig' with params %}